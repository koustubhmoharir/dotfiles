gui:
  theme:
    selectedLineBgColor:
      - "#404040"
    selectedRangeBgColor:
      - "#404040"
git:
  autoFetch: false
  pagers:
    - colorArg: always
      pager: delta --dark --side-by-side --paging=never
